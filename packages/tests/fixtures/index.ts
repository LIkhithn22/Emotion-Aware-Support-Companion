/**
 * Fixtures index — exports all per-UC seed helpers.
 *
 * Each UC will add its own seed function here.
 * Called from Cucumber step definitions or tagged Before hooks.
 *
 * TODO: populate as each UC is implemented.
 */

// UC01 fixture will be: export { seedUC01 } from './UC01_seed';
// UC02 fixture will be: export { seedUC02 } from './UC02_seed';
// ... and so on for each use case

export {};  // placeholder — remove when first UC seed is added
